#include <iostream>
using namespace std;

int main()
{
    int number;
    cout<<"Enter Number : ";
    cin>>number;

    //cout<<(number % 2 == 0) ? "Even" : "Odd";

    //cout<<((number % 2 == 0) ? "Even" : "Odd");

    //string numberType;
    //(number % 2 == 0) ? numberType="Even" : numberType="Odd";

    //cout<<numberType;


    return 0;
}
